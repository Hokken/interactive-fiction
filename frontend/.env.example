# Backend API Configuration
VITE_BACKEND_URL=http://localhost:3001

# NOTE: Never put an API key in a VITE_-prefixed variable.
# Vite inlines those into the client bundle, making them public to every visitor.
# All AI keys belong in backend/.env and are used server-side only.
